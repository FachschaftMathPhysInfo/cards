import 'package:flutter/material.dart';

class DataEntryElement extends StatelessWidget {
  const DataEntryElement({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [],
    );
  }
}
