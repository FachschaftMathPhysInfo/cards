query decks($search: String, $year: Int, $semester: String) {
  decks(search: $search, year: $year, semester: $semester) {
    subject
    module
    moduleAlt
    examiners
    language
    semester
    year
    hash
    fileType
    isValid
  }
}
